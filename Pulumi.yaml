name: comgas-service
description: A minimal Azure API service using Python for Comgas
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: azure-python
